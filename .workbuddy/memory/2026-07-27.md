# 2026-07-27 工作日志

## 代码 Review 与死代码清理 (P0+P2)

老大让 review 项目代码，着重查多轮迭代后的冗余/死代码与冗余 CSS，约束：逻辑与样式不变。

### 技术栈 (实际项目)
React 18 + TS + Vite 6 + Capacitor 6 + Dexie 4 + ECharts。
注: SOUL.md/IDENTITY.md 设定 Vue 3 基准，但当前 xiangdangdang 工程是 React 栈，按项目实际为准。

### 清理清单 (8 处，0 行为变更)
P0 死代码/冗余注释 (5):
1. styles.css L295-297 `.wrap` 工具类 — 0 处 JSX 引用，删除整块。
2. utils/time.ts L32-41 `timeAgo()` — Dashboard 已用 `timeAgoShort`，0 调用，删除。
3. db.ts L48-51 `getSetting()` — useSettings 走 `db.settings.toArray()`，0 调用，删除 (setSetting/SETTING_KEYS 保留)。
4. utils/growth.ts `export { WHO_GIRLS };` — 外部无 import，删除 re-export (保留 `export type { GrowthMetricKey }`)。
5. utils/growth.ts L32-40 双 JSDoc 块 — 第二块覆盖第一块，合并为单一 JSDoc。

P2 冗余 export 关键字 (3):
6. db.ts `export interface SettingRecord` → `interface` (仅文件内 EntityTable 用)。
7. components/ui.tsx `export interface SegmentOption` → `interface` (仅 SegmentProps 内部消费)。
8. hooks/useSettings.ts `export interface BabySettings` → `interface` (外部靠返回值推断)。

### 验证
`tsc -b && vite build` 退出码 0，631 modules transformed，CSS 16.83 kB / JS 1.37 MB (gzip 457 kB)。
chunk size > 500kB 警告是 echarts 体积问题，预先存在，与本次清理无关。

### 未清理 (跨模块重构范畴，本轮不动)
- aiContext.ts 的 `FEED_LABEL` 与 ui.tsx 的 `FEED_META.label` 字面重复，可让 aiContext 复用 FEED_META.label，但属跨模块复用重构。
- icons.tsx 26 个图标全部被引用，无死图标。
- styles.css 其余 ~80 个 class / 7 个 @keyframes 均有 JSX 引用。

### 关键经验
1. review 时先做"导出符号 → 引用"交叉验证再动手，避免误删。
2. Edit 失败时多半是 old_string 不匹配（含 JSDoc 时易记错所在文件），重读目标文件即可。
3. 双 JSDoc 块是迭代残留，第二块会覆盖第一块，肉眼不易发现，是高频冗余点。
