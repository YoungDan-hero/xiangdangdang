import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.xiangxiang.dangdang",
  appName: "响当当",
  webDir: "dist",
  android: {
    backgroundColor: "#fff5f8",
  },
};

export default config;
